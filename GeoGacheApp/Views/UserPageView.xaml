<UserControl x:Class="Geocache.Views.UserPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Geocache.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel VerticalAlignment="Stretch">
        <TextBlock Grid.Row="1">First name</TextBlock>
        <TextBox Name="firstNameTxtBox" Text="{Binding FirstName,Mode=TwoWay}" Grid.Row="2"></TextBox>
        <TextBlock Grid.Column="1" Grid.Row="1">Last name</TextBlock>
        <TextBox Name="lastNameTxtBox" Text="{Binding LastName,Mode=TwoWay}" Grid.Column="1" Grid.Row="2"></TextBox>

        <TextBlock >Adress</TextBlock>
        <TextBox Text="{Binding Adress,Mode=TwoWay}" Name="adressTxtBox"></TextBox>

        <TextBlock >City</TextBlock>
        <TextBox Text="{Binding City,Mode=TwoWay}" Name="cityTxtBox" ></TextBox>

        <TextBlock >Country</TextBlock>
        <TextBox Text="{Binding Country,Mode=TwoWay}" Name="countryTxtBox"  ></TextBox>
        <TextBlock >Created at</TextBlock>
        <TextBlock Text="{Binding createdAt,Mode=TwoWay}"></TextBlock>

        <Button Command="{Binding SaveChanges}">Save changes</Button>
    </StackPanel>
</UserControl>
